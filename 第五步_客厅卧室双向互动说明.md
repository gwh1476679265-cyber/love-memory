# 小屋 Phase 5：客厅 / 卧室双向互动

这版建立在你现在已经跑通的 CloudBase、PostgreSQL、Git 自动部署、iPhone 访问提醒之上。

## 这次完成的功能

1. **“我们的约定”可以新建了**
   - 原来的打勾同步保留。
   - 卧室中新增“＋ 新约定”输入框。
   - 任意一方创建后写入 PostgreSQL，另一台设备约 4 秒内自动看到。

2. **客厅时间轴最下方改成双人留言板**
   - 原来的“未拆封彩蛋”卡片已删除。
   - 双方都能留言。
   - 留言保存到 `couple_messages`，双方都能看到。
   - 自己写的显示为“我”，另一台设备上会显示为“TA”。
   - 当前显示最近 60 条留言。

3. **留言板下方新增“敲门进入卧室”**
   - 这是现在从客厅进入卧室的唯一入口。

4. **取消点击顶部标题跳转**
   - 顶部牌子只负责显示“客厅”和当前回忆日期。

5. **主标题更名**
   - `恋爱回忆` → `客厅`
   - `关于边芸芸的一切` → `卧室`
   - 卧室左上返回按钮文字也改成“客厅”。

## 你现在只做两步

### 第一步：执行 SQL

CloudBase → SQL 型数据库 → 新建查询，整段执行：

`05_客厅留言板与新约定.sql`

它不会清空你已经存在的 25 条约定，也不会改掉已经保存的勾选状态。

它只会：

- 给 `couple_todos` 增加前端创建权限；
- 新建 `couple_messages` 留言表；
- 配置留言读取 / 新增的 RLS 权限。

### 第二步：GitHub cloud 分支替换三个文件

替换：

- `index.html`
- `script.js`
- `style.css`

然后 Commit + Push。

你已经配置好的 GitHub → CloudBase 自动部署会自己发布。

**不需要动：**

- `images/`
- `music/`
- 贺卡 WebP
- `cloudbase-config.js`
- `sw.js`
- `love-house-notify` 云函数
- VAPID / API Key

## 测试方法

### 新约定

设备 A 打开“卧室”，输入“下次一起去看海”，点“＋ 新约定”。

设备 B 停留在卧室，大约 4 秒内应该出现这条新约定；之后双方都可以打勾，刷新也保留。

### 留言板

设备 A 在客厅滑到最下面，写一句留言。

设备 B 停留在客厅最下方，大约 4 秒内应该看到新留言。

### 页面跳转

顶部“客厅”牌子现在点了不会跳转。

只有留言板下方的“敲门进入卧室”会进入卧室。
